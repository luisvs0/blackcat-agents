# protohello-quickjs

**experimental/internal** use only

`protohello-quickjs` provides a QuickJS-backed REPL middleware for Black Cat. It adds a `repl` tool that can evaluate small JavaScript snippets for computation, control flow, JSON manipulation, and calls to exposed Python foreign functions.

