# Ensures version numbers in pyproject.toml and _version.py stay in sync.
#
# (Prevents releases with mismatched version numbers)

name: "🔍 Check Version Equality"

on:
  pull_request:
    paths:
      - "libs/blackcat/pyproject.toml"
      - "libs/blackcat/blackcat/_version.py"
      - "libs/cli/pyproject.toml"
      - "libs/cli/blackcat_cli/_version.py"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  check_version_equality:
    runs-on: ubuntu-latest
    timeout-minutes: 2

    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3

      - name: "🐍 Set up Python and uv"
        uses: "./.github/actions/uv_setup"
        with:
          python-version: "3.14"
          enable-cache: "false"

      - name: "✅ Verify pyproject.toml & _version.py Match"
        run: python .github/scripts/check_version_equality.py
